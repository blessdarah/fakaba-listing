# Signup feature

## Flow 
1. User clicks on signup button
2. User is redirected to the signup screen
3. User fills in the form and submits
4. User is redirected to setup screen

## Instructions
- Move the setup stack to it's own stack level as the signup
- Prevent redirect to (tabs) after signup and instead redirect to setup
- After setup is complete, redirect to (tabs) 
